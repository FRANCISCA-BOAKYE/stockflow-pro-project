import { CreditScreen } from '../../../components/CreditScreen';

export default function RetailerCreditScreen() {
  return (
    <CreditScreen
      subtitle="Customer credit accounts"
      emptySubtext="Credit accounts will appear here when created via POS"
    />
  );
}
